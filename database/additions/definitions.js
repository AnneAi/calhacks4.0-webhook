const definitions = [
  {
    level: '*',
    definition: `Addition is finding the total, or sum, by combining two or more numbers. Example: 5+11=16 is an addition`
  }
];

module.exports = definitions;
