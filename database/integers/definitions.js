const definitions = [
  {
    level: '*',
    definition: `Integers can be negative {-1, -2,-3, -4, -5, ... }, positive {1, 2, 3, 4, 5, ... }, or zero {0}. There is no fractional or decimal part`
  }
];

module.exports = definitions;
