const definitions = [
  {
    level: '*',
    definition: 'Whole numbers are simply the numbers 0,1,2,3, 4, 5, ... (and so on). There is no fractional or decimal part and no negatives (a negative number has a minus before the number)'
  }
];

module.exports = definitions;
